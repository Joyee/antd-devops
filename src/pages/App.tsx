import { PageContainer } from '@ant-design/pro-components';
import React from 'react';

const Application: React.FC = () => {
  return <PageContainer>应用</PageContainer>;
};

export default Application;
